\documentclass[12pt]{article}

% This is a template file for Parabola articles.
% The main contents are given by the file ``sample_article.tex" inputted below.
% - Thomas Britz 2018-11-23
\usepackage{listings}

\usepackage{latexsym,amsthm,amssymb,amsmath,amsfonts,graphicx,palatino}
\usepackage{fancybox,multicol,array,psfrag,longtable}
\usepackage[colorlinks=true,citecolor=black,linkcolor=black,urlcolor=blue]{hyperref}
\usepackage{verbatim,color,tabularx,cite}
\usepackage{bm}% bold math
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{graphicx}
\usetikzlibrary{calc,intersections,through,patterns}

\allowdisplaybreaks

\textwidth16cm
\textheight22.5cm
\topmargin-1cm
\evensidemargin0cm
\oddsidemargin0cm

\renewcommand{\theequation}{\arabic{equation}}
\renewcommand{\thesubsection}{\arabic{section}.\arabic{subsection}}

\newtheorem{task}{Task}
\newenvironment{solution}{\renewcommand{\proofname}{Solution}\begin{proof}}{\end{proof}}

\newtheorem{theorem}{Theorem}[]
\newtheorem*{theorem*}{Theorem}
\newtheorem*{example*}{Example}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{definition}[theorem]{Definition}
\newtheorem*{definition*}{Definition}
\newtheorem*{notation*}{Notation}
\newtheorem{example}{Example}[]
\newtheorem*{quest*}{Question}
\def\proof{\noindent {\it Proof. $\, $}}
\def\endproof{\hfill $\Box$ \vskip 5 pt }

\newcommand{\tzontz}[1]{\bigskip\noindent{\bf $2\mathbb{Z}$ Or Not $2\mathbb{Z}$: #1}}
\newcommand{\problem}[1]{\bigskip\noindent{\bf Problem #1}\newline}
%\newcommand{\solution}[1]{\bigskip\noindent{\it Solution#1}  }
\def\Soln{\par\noindent{\bf SOLUTION}  }
\def\Correctsoln{\par\noindent{\bf Correct solution}}
\def\Partialsoln{\par\noindent{\bf Partial solution}}
\def\Nowtry{\par\noindent{\bf NOW TRY} Problem\ }
\def\spreadrels{\advance\thickmuskip by\medmuskip}
\newcommand{\ph}[1]{\phantom{#1}}

\newcounter{question}
\newenvironment{question}
   {\begin{list}{\bf Q\thequestion\;\;}{\usecounter{question}
       \setlength{\rightmargin}{0.0cm}
       \setlength{\leftmargin}{0.0cm}
       \setlength{\itemsep}{5pt}
       \setlength{\topsep}{0.3cm}
       \setlength{\itemindent}{1.0cm}
       \setlength{\listparindent}{0.5cm}
   }}{\end{list}}

\newcommand{\similar}{\mathrel{\vert\vert\vert}}
\DeclareMathOperator{\cosec}{{cosec \/}}
\DeclareMathOperator{\lcm}{lcm}

\DeclareGraphicsRule{.eps}{mps}{*}{}

%\newtheorem*{theorem}{Theorem}

\newenvironment{fcentre}{\par\kern
  5pt \begin{center}}{\end{center}\kern 5pt}

\renewcommand{\theenumi}{\alph{enumi}}
\renewcommand{\labelenumi}{(\theenumi)}
\renewcommand{\labelenumii}{\theenumii)}

\newtheoremstyle{problem} % name of the style to be used
 {.5em}% measure of space to leave above the theorem. E.g.: 3pt
 {.1em}% measure of space to leave below the theorem. E.g.: 3pt
 {}% name of font to use in the body of the theorem
 {}% measure of space to indent
 {}% name of head font
 {:}% punctuation between head and body
 {.5em}% space after theorem head; " " = normal interword space
 {\textbf{\thmname{#1}\thmnumber{ #2}}\thmnote{ [#3]}}% Manually specify head

\theoremstyle{problem}

%\newtheorem{problem}{Problem}[section]

\renewcommand{\thesection}{\Alph{section}}
%\renewcommand{\theproblem}{\thesection\arabic{problem}}

\def\mathstrut{\vrule height10pt depth 5pt width0pt}

\long\def\Problem#1\Solution#2\Endproblem{
  \begin{problem}
    #1
  \end{problem}}


\makeatletter

% define a macro \Autoref to allow multiple references to be passed to \autoref
\newcommand\Autoref[1]{\@first@ref#1,@}
\def\@throw@dot#1.#2@{#1}% discard everything after the dot
\def\@set@refname#1{%    % set \@refname to autoefname+s using \getrefbykeydefault
    \edef\@tmp{\getrefbykeydefault{#1}{anchor}{}}%
    \def\@refname{\@nameuse{\expandafter\@throw@dot\@tmp.@autorefname}s}%
}
\def\@first@ref#1,#2{%
  \ifx#2@\autoref{#1}\let\@nextref\@gobble% only one ref, revert to normal \autoref
  \else%
    \@set@refname{#1}%  set \@refname to autoref name
    \@refname~\ref{#1}% add autoefname and first reference
    \let\@nextref\@next@ref% push processing to \@next@ref
  \fi%
  \@nextref#2%
}
\def\@next@ref#1,#2{%
   \ifx#2@ and~\ref{#1}\let\@nextref\@gobble% at end: print and+\ref and stop
   \else, \ref{#1}% print  ,+\ref and continue
   \fi%
   \@nextref#2%
}

\makeatother

   \renewcommand{\chapterautorefname}{Chapter}
   \renewcommand{\sectionautorefname}{Section}
   \newcommand{\algocflineautorefname}{Algorithm}
   \newcommand{\taskautorefname}{Task}
   \newcommand{\subfloatautorefname}{Subfigure}

%\usepackage[caption=false]{subfig}
\usepackage{tikz}
   \tikzset{>=stealth}
   \usetikzlibrary{trees,arrows}
   \usetikzlibrary{positioning}
   \usetikzlibrary{chains}
\usepackage{pdfpages}
   \pdfminorversion=7
\usepackage{mdframed}
\usepackage{booktabs}
   \renewcommand{\arraystretch}{1.3}

\usepackage{mathtools}
   \DeclarePairedDelimiter{\floor}{\lfloor}{\rfloor}
   \DeclarePairedDelimiter{\ceil}{\lceil}{\rceil}
   \DeclarePairedDelimiter{\abs}{\lvert}{\rvert}
   \DeclarePairedDelimiter{\size}{\lvert}{\rvert}
   \DeclarePairedDelimiter{\set}{\{}{\}}
   \DeclarePairedDelimiterX\Set[2]{\lbrace}{\rbrace}{ #1 \, \colon \,\mathopen{} #2 }
   \DeclarePairedDelimiter{\norm}{\lVert}{\rVert}
   \DeclarePairedDelimiter{\ip}{\langle}{\rangle}
   \newcommand{\innerprod}[2]{\ip*{#1,#2}}

% Other
\usepackage[utf8]{inputenc}
\emergencystretch=1em
\usepackage{cleveref}
% \usepackage[a4paper,total={6in,8in}]{geometry}

\newcommand{\Z}{\mathbb{Z}}
\newcommand{\R}{\mathbb{R}}


\begin{document}

\noindent {\bf Parabola Volume 55, Issue 2 (2019)}0
\input{sample_article} %
\end{document}

